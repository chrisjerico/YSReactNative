import * as React from "react";
import {BaseScreen} from "./component/BaseScreen";

export const LCPromotionsPage = () => {
    return (
        <BaseScreen screenName={'优惠活动'} icon={'home'}>
        </BaseScreen>
    )
}
