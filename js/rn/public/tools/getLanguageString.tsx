import { OCHelper } from "../define/OCHelper/OCHelper"

export async function getLanguageCode(stringKey) {
  // const value = await OCHelper.call("LanguageHelper.shared.stringForKey:", [stringKey])
  // return value
  // return
}